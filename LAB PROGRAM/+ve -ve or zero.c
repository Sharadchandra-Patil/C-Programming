#include<stdio.h>
int main()
{
    int no=0;
    
     if(no>0)
        printf("\n %d is +ve", no);
     else if  (no==0)
        printf("%d no is zero", no);
    else 
        printf("%d is -ve", no);


    return 0;
}